{
  "name": "flomo-speech",
  "version": "1.0.0",
  "description": "A voice-to-text app with Flomo integration",
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npm start",
    "dev:server": "cd server && npm run dev",
    "build": "cd client && CI=false npm run build",
    "start": "cd server && npm start",
    "vercel-build": "cd client && npm install --legacy-peer-deps && CI=false npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
